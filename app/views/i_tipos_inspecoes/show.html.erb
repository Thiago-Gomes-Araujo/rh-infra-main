<div class="container">
  <%= render "shared/notice" %>
  <div class="card border-0 shadow shadow-sm">
    <div class="card-header border-0 d-flex justify-content-between">
      <span class="fs-4">Visualizar I Tipo Inspecao</span>
    </div>
    <div class="card-body">
      <div id="<%= dom_id @i_tipo_inspecao %>">
              
          <p>
            <strong>Descricao:</strong>
            <%= @i_tipo_inspecao.descricao %>
          </p>
              
          <p>
            <strong>Created By:</strong>
            <%= @i_tipo_inspecao.created_by %>
          </p>
              
          <p>
            <strong>Updated By:</strong>
            <%= @i_tipo_inspecao.updated_by %>
          </p>
              
          <p>
            <strong>Deleted At:</strong>
            <%= @i_tipo_inspecao.deleted_at %>
          </p>
     
      </div>
    </div>
    <div class="card-footer border-0 d-flex justify-content-between">
      <div>
        <%= link_to edit_i_tipo_inspecao_path(@i_tipo_inspecao), class: "btn btn-primary" do %>
          <i class="bi bi-pencil-square"></i> Editar
        <% end %>
        <%= link_to "Voltar", i_tipos_inspecoes_path, class: "btn btn-secondary" %>
      </div>
      <%= button_to "Deletar", @i_tipo_inspecao, method: :delete, class: "btn btn-outline-danger border-0" %>
    </div>
  </div>
</div>
